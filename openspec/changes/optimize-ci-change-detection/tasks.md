## 1. 范围与设计

- [x] 1.1 A 根据历史 workflow 和运行时长确认问题来自无条件触发，并保留现有质量门内容。
- [x] 1.2 A 确认不使用顶层 `paths` 直接跳过必需 workflow，采用 job 内变更检测。

## 2. 实现

- [x] 2.1 A 新增共用 Git 变更检测脚本，异常时安全回退为完整验证。
- [x] 2.2 A 为 Frontend Verify 增加前端范围条件和同组运行取消。
- [x] 2.3 A 为 Backend Verify 增加后端范围条件和同组运行取消。

## 3. 验证

- [x] 3.1 A 验证纯文档范围使前后端均输出 `run=false`。
- [x] 3.2 A 验证纯前端、纯后端和混合范围只执行对应质量门。
- [x] 3.3 A 验证 workflow/检测脚本自身变更与无有效 base SHA 均输出 `run=true`。
- [x] 3.4 A 完成 workflow YAML 解析、Shell 语法、OpenSpec 严格校验和 Git 差异检查。

## 4. 后续

- [x] 4.1 A 通过临时纯文档 PR #35 确认前后端必需检查正常创建并成功，且所有重型步骤均跳过；PR 验收后关闭且未合并。
