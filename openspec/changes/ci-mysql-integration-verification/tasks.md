## 1. 范围与安全边界

- [x] 1.1 A 确认 CI 只使用一次性 MySQL 8.4，不连接共享数据库，不读取生产或团队日常凭据。（验证：proposal、design 和 spec 明确隔离边界）
- [x] 1.2 确认本 change 不修改 Flyway SQL、业务实现、REST、权限或部署行为。（验证：Git 差异仅包含 workflow、测试隔离配置和 OpenSpec）

## 2. CI 与测试实现

- [x] 2.1 新增 MySQL 8.4 服务、上海时区、健康检查、临时账号和固定隔离数据库。（验证：workflow 静态解析通过）
- [x] 2.2 开启五个真实 MySQL 测试并显式运行测试集合。（验证：环境变量和 Maven 测试类清单核对通过）
- [x] 2.3 场次查询测试改用 Flyway、固定种子和业务时钟，并在查询前校验隔离数据库名；交易测试统一断言 MySQL 8.4，生命周期测试验证 V005 成功存在而非固定为最新版本。（验证：测试代码审查与真实 MySQL 执行）

## 3. 验证与交付

- [x] 3.1 OpenSpec 严格校验、`git diff --check` 和后端完整 `verify` 通过；后端 65 个测试中 60 通过、0 失败、5 个外部环境测试按门控跳过。
- [x] 3.2 一次性 MySQL 8.4.11 上五个集成测试全部通过：5 通过、0 失败、0 错误、0 跳过。
- [ ] 3.3 推送分支后 GitHub Actions 的 MySQL 服务健康和集成质量门通过。
