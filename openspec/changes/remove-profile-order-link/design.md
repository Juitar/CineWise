## 实现方案

个人中心删除指向 `/orders` 的 `Link`，并删除仅服务该链接的 `.profile-orders-link` CSS 规则。隐私说明和退出登录保持原样。

订单入口继续由公共导航负责：桌面侧边栏、桌面用户菜单和移动端底部导航均不在本 change 的修改范围内。

## 测试方案

- 个人中心组件测试断言已登录用户不再看见“我的订单”，仍能看见隐私说明链接。
- 执行相关 Vitest、OpenSpec 严格校验和 Git 差异检查。

## 回退方式

回退本 change 的个人中心、测试和 OpenSpec 文件即可，不涉及接口或订单数据。
