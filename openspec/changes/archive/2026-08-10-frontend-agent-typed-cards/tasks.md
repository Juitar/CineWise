# 任务

- [x] C：扩展安全投影视图模型，仅加入正式 DTO 中允许展示的问题选项等白名单字段；用投影单测验证。
- [x] C：实现七类类型化卡片、统一注册入口和响应式样式；用组件单测验证。
- [x] C：将 Agent 工作区切换到统一卡片入口，保留确认回调和现有网络状态；用工作区测试验证。
- [x] C：补桌面和移动浏览器场景，验证推荐卡、确认卡和安全展示。
- [x] C：运行 `pnpm check`、相关 Playwright、OpenSpec 严格校验、后端仓库要求检查和 `git diff --check`，记录未验证项。
