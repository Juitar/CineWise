## 1. 规则和迁移确认

- [x] 1.1 C 对照 PRD、认证设计、后端总系分、现有密码登录实现确认发送与验证码登录范围。
- [x] 1.2 C 建立 `email-code-authentication` proposal、spec、design、tasks 和迁移申请；验证：OpenSpec 严格校验。
- [x] 1.3 结构迁移版本已确定为 V011；A 已完成 T02/T03/T04、登录日志 CHECK、索引、约束和兼容性审查，未创建邀请码种子迁移。

## 2. 验证码领域和应用服务

- [x] 2.1 C 实现用途、状态、生成器、HMAC 摘要和配置校验；验证：6 位数字、摘要用途隔离、密钥长度和固定规则测试通过。
- [x] 2.2 C 实现发送限流端口、投递端口和验证码 Repository；验证：接口只传摘要和必要认证数据。
- [x] 2.3 C 实现验证码发送服务和事务辅助；验证：未知账号统一成功、重复窗口不投递、明确失败失效、未知结果不重发测试通过。
- [x] 2.4 C 实现验证码登录和一次性消费；验证：正确、错误、上限条件、已使用、并发条件失败和管理员拒绝测试通过。

## 3. API 和基础设施

- [x] 3.1 C 增加发送和验证码登录 DTO、Controller、错误码、Cookie/CSRF 与 OpenAPI；验证：4 项 Controller 集成测试通过。
- [x] 3.2 C 实现 MyBatis Mapper/Repository；验证：H2 前向结构夹具验证条件消费和无明文存储；正式迁移仍等待 A。
- [x] 3.3 C 实现 Redis 冷却/频率限制 Adapter；验证：4 项单元测试覆盖原子计数、冷却 TTL、IP 拒绝和精确释放；真实 Redis 留在 4.3。
- [x] 3.4 C 实现配置化 SMTP Adapter 和未配置失败关闭 Adapter；验证：3 项单元测试覆盖成功、明确失败和结果未知；真实收信留在 4.3。
- [x] 3.5 C 增加 465 SSL、STARTTLS required 和启动连接检查环境变量；验证：真实 `application.yml` 环境绑定测试。

## 4. 迁移和真实环境

- [x] 4.1 A 已审核并发布 `V011__create_auth_email_code_and_registration_tables.sql`；V011 只包含结构和前向 CHECK，C 未私自执行共享迁移。
- [x] 4.2 GitHub Actions 已在一次性 MySQL 8.4 验证首次迁移、结构、索引和 CHECK；共享或生产数据库仍需部署负责人确认。
- [ ] 4.3 C/A 在真实 Redis 和真实测试邮箱验证一次投递、冷却、登录、结果未知与敏感信息检查。

## 5. 注册实现

- [x] 5.1 C 实现注册 DTO、密码/昵称/隐私政策校验和邀请码摘要；验证：正常、字段边界和敏感信息测试。
- [x] 5.2 C 实现邀请码 Repository、条件扣减和使用记录；验证：无效、未生效、过期、禁用、耗尽和版本竞争。
- [x] 5.3 C 实现注册事务和重复 `clientRequestId` 安全恢复；验证：验证码、邀请码、账号和使用记录同事务回滚，重放参数不一致被拒绝。
- [x] 5.4 C 接入 `POST /api/v1/auth/register`、Cookie、CSRF、错误码和 OpenAPI；验证：Controller 集成测试。
- [x] 5.5 C 实现默认关闭的首个邀请码受控 Initializer 和 Git 忽略环境配置；验证：首次插入、配置失败关闭、重复启动不新增且不重置次数、敏感信息不输出。

## 6. 提交前验证

- [x] 6.1 C 完成首个邀请码 Initializer 后重新执行后端完整 Maven verify，记录通过、失败和跳过数。
- [x] 6.2 C 完成首个邀请码 Initializer 后重新执行 OpenSpec 严格校验、`git diff --check`、状态和变更范围核对。

## 7. 前端注册接入

- [x] 7.1 C 在登录页增加注册链接，并补充注册验证码和注册 DTO/API；验证：请求路径、用途、CSRF 和敏感输入测试。
- [x] 7.2 C 将 `/register` 接入认证 Hook，完成字段校验、冷却倒计时、错误码提示、重复提交和 `/auth/me` 结果恢复；验证：Hook 单元测试。
- [x] 7.3 C 删除静态预览和禁用占位，补齐邀请码、可选昵称、隐私同意及响应式表单；验证：页面组件测试。
- [x] 7.4 C 执行前端格式、Lint、类型、单测和生产构建检查，并重新执行 OpenSpec 严格校验与 `git diff --check`。

## 8. 前端邮箱验证码登录

- [x] 8.1 C 增加验证码登录 DTO/API 和 `AuthProvider` 会话更新入口；验证：请求路径、字段、CSRF 清理和 `/auth/me` 恢复测试。
- [x] 8.2 C 实现 `LOGIN` 验证码发送、冷却、错误码和结果未知恢复 Hook；验证：正常、发送未知、验证码无效、重复提交和仅查询恢复测试。
- [x] 8.3 C 在统一登录页增加密码/验证码方式切换和验证码表单；验证：切换清理敏感输入、发送、提交、安全回跳和响应式页面测试。
- [x] 8.4 C 执行前端格式、Lint、类型、单测和生产构建检查，并重新执行 OpenSpec 严格校验与 `git diff --check`。
- [x] 8.5 C 在邮箱为空或格式不合法时禁用登录验证码发送按钮，并补页面测试。
- [x] 8.6 C 将验证码发送统一成功提示改为“请检查邮箱”，不向用户暗示邮件已经实际投递。
