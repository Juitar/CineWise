# Tasks

## 1. 管理订单筛选

- [x] 1.1 A 为筛选控件补充稳定 `id` 和文本输入 `name`；验证：组件测试覆盖字段标识、查询与重置。

## 2. 内容同步反馈

- [x] 2.1 C 使同步 Hook 返回真实任务或空结果，不改变错误与结果未知恢复；验证：Hook 成功/失败测试通过。
- [x] 2.2 C 仅在取得真实任务时显示成功提示；验证：页面测试覆盖成功和失败分支。

## 3. 交付

- [x] 3.1 执行并记录交付检查；验证：OpenSpec strict、受影响文件 Prettier、3 个定向测试文件共 9 项和 `git diff --check` 通过；完整 `pnpm check` 被 `origin/dev` 中 7 个无关文件的既有 Prettier 问题阻断。
