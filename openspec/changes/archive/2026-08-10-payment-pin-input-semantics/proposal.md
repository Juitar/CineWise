# 模拟支付六码输入语义修复

## 背景

模拟支付页面使用 `Input.Password`，浏览器将六码本地校验输入误判为登录凭据并弹出保存或更新密码提示。

## 范围

- A 将模拟支付六码改为普通数字 PIN 输入。
- 保留本地六位数字校验、结果未知保护和无参数支付回调。

## 非范围

- 不修改登录/注册密码框。
- 不向支付 API、日志、存储或事件传递六码。
- 不改认证、路由或公共请求层。

## 验收

- 输入控件不具有密码类型或密码自动填充语义。
- 六位数字仅留在组件内存，触发支付前清空，页面和请求层均不可读取。
