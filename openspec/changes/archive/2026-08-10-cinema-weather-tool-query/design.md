# 设计

`GetWeatherTool` 只接收 `cinemaId`。新增 D 内部公开的影院地理查询接口，返回已登记影院的经纬度；Agent 适配器只能调用 `GetWeatherTool`。

天气服务通过高德逆地理编码将影院坐标转换为 `adcode`。逆地理编码不可用时，才使用内容数据已保存的行政区和受控映射作为兼容回退；绝不使用用户位置或模糊地址。天气仍沿用真实结果、缓存、Demo、不可用的回退顺序。

该变更不新增表和迁移。外部调用均设置连接/读取超时，第三方原始响应和坐标不写日志、缓存键或 Agent 轨迹。
