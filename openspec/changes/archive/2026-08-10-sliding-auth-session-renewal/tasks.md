# 任务

- [x] 1.1 C：检查现有 JWT、Cookie、过滤器、配置和认证 OpenSpec，确认无等价续期 change；验证：记录现状和独立范围。
- [x] 1.2 C：增加三项配置、环境变量示例和启动绑定校验；验证：合法/非法配置测试。
- [x] 2.1 C：定义包含实际到期时间的令牌结果，扩展 Token 服务首次签发和保留会话起点续签；验证：固定 Clock 单测。
- [x] 2.2 C：调整登录、邮箱验证码登录、管理员登录和注册签发带 `sessionStartedAt` 的新 JWT；验证：认证应用与 Controller 测试。
- [x] 2.3 C：调整 Cookie Manager 按实际剩余时间写 Max-Age；验证：30 分钟和 8 小时上限测试。
- [x] 2.4 C：在统一认证过滤器实现触发、排除、并发和失败处理；验证：过滤器单元/集成测试。
- [x] 2.5 C：覆盖 `/auth/me`、普通 GET/POST、OPTIONS 和认证排除接口；验证：安全集成测试。
- [x] 2.6 C：验证 POST SSE 建流前写 Cookie且不修改 Agent 业务；验证：认证过滤器链测试。
- [x] 2.7 C：验证登出、账号状态/角色/版本变化和密码重置使旧/续签 JWT 失效；验证：认证与密码重置集成测试。
- [x] 2.8 C：覆盖日志、响应和 Cookie 敏感信息检查；验证：日志捕获与响应断言。
- [x] 3.1 C：执行续期定向测试、认证 Controller/安全测试和密码重置测试；验证：记录通过、失败、跳过数。
- [x] 3.2 C：执行 `backend\\mvnw.cmd verify`；Wrapper 失败时记录原因并用同版本 `mvn verify`；验证：完整构建通过。
- [x] 3.3 C：执行本 change、密码重置、公共邮件及既有认证 change strict 校验；验证：全部通过。
- [x] 3.4 C：执行 `git diff --check`、状态和 OpenAPI/配置/环境变量核对；验证：只包含当前认证任务改动。
- [ ] 3.5 C/部署负责人：验证真实 HTTPS 代理保留续签 `Set-Cookie` 和 POST SSE 建连响应头；验证：记录环境、时间和未验证项。
