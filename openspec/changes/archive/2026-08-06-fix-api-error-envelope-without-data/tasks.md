## 1. 契约与实现

- [x] 1.1 C 分离公共失败信封和成功数据校验，失败响应允许省略 `data` 或为 null，成功响应仍要求存在 `data`；验证：公共客户端成功/失败边界测试通过且无 `as T` 掩盖。
- [x] 1.2 C 保留 `ApiError` 的 `status/code/traceId/message`，并为不可解析响应使用固定安全文案；验证：401、403、409、422、429、5xx 及非法响应测试通过，测试与实现不输出完整响应体。
- [x] 1.3 C 调整 401 和 403 公共处理：并发 401 单次处理，201009 清理并刷新 CSRF 但不重放，201007 不清理、不刷新、不跳登录；验证：对应并发和请求次数断言通过。

## 2. 调用方回归

- [x] 2.1 C 运行并补充认证请求层相关测试，确认认证模块收到公共 `ApiError` 且不改写状态、错误码和 traceId；验证：认证 API 与 AuthProvider 定向测试通过。
- [x] 2.2 C 在不修改 A 退款实现的前提下，为现有 `createRefund()` 增加 403/201009、401/201006 和 409 错误回归；验证：调用方收到真实 `ApiError`，消息不是“服务返回的数据格式不正确”。

## 3. 验证

- [x] 3.1 C 运行公共客户端、认证请求层和退款定向测试；验证：4 个测试文件、39 个测试通过，0 失败，0 跳过。
- [x] 3.2 C 运行 `pnpm check` 和生产构建检查；验证：同步最新 `origin/dev` 后格式、Lint、类型检查通过，74 个测试文件、315 个测试通过，生产构建和 86 个哈希 JS/CSS 产物检查通过。
- [x] 3.3 C 执行 `openspec validate fix-api-error-envelope-without-data --strict`、`git diff --check`、`git status --short --branch` 和变更文件范围检查；验证：严格校验和差异检查通过，分支与当前 `origin/dev` 同为 `969f435`，只包含公共客户端、公共测试、退款回归测试和本 change OpenSpec。
